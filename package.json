{
  "name": "lerna",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "lerna": "^3.22.1",
    "release-it": "^14.2.1"
  },
  "scripts": {
    "test": "echo 'no tests defined'",
    "release": "release-it --dry-run"
  }
}
